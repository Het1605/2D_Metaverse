<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/voice_chat_popup_bg"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center"
    android:weightSum="1">

    <!-- Group icon and title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="start|center_vertical"
        android:layout_marginBottom="20dp">

        <ImageView
            android:id="@+id/group_icon"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:src="@drawable/group_call"
            android:layout_marginEnd="16dp" />


    </LinearLayout>

    <!-- Container for dynamic user divs -->
    <LinearLayout
        android:id="@+id/users_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_weight="1">
        <!-- User divs will be dynamically added here -->
    </LinearLayout>

    <!-- End Call Button with icon -->
    <ImageButton
        android:id="@+id/end_call_button"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:background="@drawable/voice_button_background"
        android:src="@drawable/call"
        android:scaleType="centerInside"
        android:padding="16dp"
        android:contentDescription="Voice Chat Button"
        android:elevation="6dp" />

</LinearLayout>
